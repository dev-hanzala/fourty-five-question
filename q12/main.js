var friendList = ["Adil", "Irtiza", "Talha", "Imran"]; // Store the name of my friends in an array.
for (var i = 0; i < friendList.length; i++) {
    console.log("".concat(friendList[i], ", How are you?"));
} // Print each friend a message.
