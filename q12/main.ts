let friendList:String[] = ["Adil", "Irtiza", "Talha", "Imran"]; // Store the name of my friends in an array.

for(let i = 0; i<friendList.length; i++){
	console.log(`${friendList[i]}, How are you?`);
} // Print each friend a message.
