var users = ["Admin", "RegularUser1", "RegularUser2"];
users.forEach(function (users) {
    if (users == "Admin") {
        console.log("Hello Admin.");
    }
    else {
        console.log("Hello User.");
    }
    ;
});
